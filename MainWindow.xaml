<Window x:Class="AfrrCollector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="aFRR Data Collector" Height="760" Width="1200">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="120" />
            <RowDefinition Height="*" />
            <RowDefinition Height="280" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Regions" FontWeight="Bold" Margin="0,0,0,6" />
                    <ListBox x:Name="RegionsListBox" SelectionMode="Multiple" Height="130" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="From" FontWeight="Bold" Margin="0,0,0,6" />
                    <DatePicker x:Name="FromDatePicker" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10,0,0,0">
                    <TextBlock Text="To" FontWeight="Bold" Margin="0,0,0,6" />
                    <DatePicker x:Name="ToDatePicker" />
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="10,0,0,0">
                    <TextBlock Text="Direction" FontWeight="Bold" Margin="0,0,0,6" />
                    <ComboBox x:Name="DirectionComboBox" />
                </StackPanel>

                <StackPanel Grid.Column="4" Margin="10,0,0,0">
                    <TextBlock Text="&quot;Rows/day&quot; hint" FontWeight="Bold" Margin="0,0,0,6" />
                    <TextBlock Text="NUCS URL currently uses 100 rows/day." TextWrapping="Wrap" Foreground="#666" />
                </StackPanel>

                <StackPanel Grid.Column="5" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button x:Name="FetchButton" Width="140" Height="32" Content="Fetch Data" Click="FetchButton_OnClick" />
                    <TextBlock x:Name="StatusText" Margin="0,8,0,0" Foreground="#666" Text="Ready." />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Debug / Last error" FontWeight="Bold" Margin="0,0,0,6" />
                <TextBox x:Name="ErrorMessageTextBox"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         Height="86" />
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2"
                  x:Name="ResultsDataGrid"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Day" Binding="{Binding Day}" Width="100" />
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="80" />
                <DataGridTextColumn Header="Total MW" Binding="{Binding TotalMw, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="Price Avg" Binding="{Binding PriceAvg, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="Price Min" Binding="{Binding PriceMin, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="Price Max" Binding="{Binding PriceMax, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume, StringFormat=N2}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="8">
            <oxy:PlotView x:Name="DailyVolumePlot" />
        </Border>
    </Grid>
</Window>
